'use client';

import { Shell } from '@/components/shell';

import { StoplistView } from './view';

export default function StoplistPage() {
  return (
    <Shell>
      <StoplistView />
    </Shell>
  );
}
